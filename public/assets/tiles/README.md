# Tile templates

JanCame generates 34 tile template images at runtime in `src/recognition/templates.ts`.
Replace with photographed real tile assets under this directory for better recognition accuracy.

Expected naming: `1m.png`, `2m.png`, … `9m.png`, `1p.png`, … `C.png`
